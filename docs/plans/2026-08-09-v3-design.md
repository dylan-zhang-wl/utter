# Utter v3 设计：本地优先的可分发双模语音平台

**日期：** 2026-08-09
**修订：** 2026-08-10 增补 §4.1（听写模式完整规格）并修正 §4 关于 VAD 的错误论断；§8 将 P2 拆为 P2a / P2b。
**项目更名：** LiveScribe → **Utter**（2026-08-09 定）。v1/v2 文档中的 "LiveScribe" 均指本项目旧名。
**取代：** `2026-04-14-livescribe-v2-design.md` 与 `2026-04-14-livescribe-v2-implementation.md`
**语言说明：** 本文是决策文档，用中文；后续的 TDD 实现计划仍按 v1/v2 惯例用英文写（那是给 Claude 执行的规格）。

## 1. 为什么改版

v2 冻结于 2026-04-15，此后四个月项目休眠。三件新事实使 v2 的设计前提失效：

1. **本机已有 `mlx-community/whisper-large-v3-turbo`（1.5G，MLX 格式）**，2026-08-01 因 digital-human 项目下载。它跑 Apple Silicon GPU，精度与速度都远超 v1 用的 faster-whisper `small`（CPU / int8）。v2 设计文档自列的痛点之三"非母语者准确率一般"，其原方案是 base→small，属杯水车薪；换 large-v3-turbo 才是对症。
2. **实测推翻了 v2 的 partial 管线**（见 §3）。v2 的核心管线设计不可用。
3. **目标从"我一台 M2 上的听课工具"扩展为"可分发的双模平台"**：既做实时听（讲座/会议转录+翻译），也做说（听写注入光标）；模型层可插拔；能给自己的其他设备和同学使用，由他们自选 STT 模型与 LLM。

第 3 条是根本性的——v2 通篇假设单模式、单 provider、单机器，这个假设整个不成立了。

## 2. 目标与非目标

**目标**

- 一套底座、两种模式，模式之间共享音频、模型、配置、历史。
- **本地优先**：默认全链路离线（本地 STT + 本地 LLM），未刊稿与保密译稿不出机器。
- **可插拔**：STT 与 LLM 都是 provider 接口，用户自选下载哪个模型、接哪个 LLM。
- **可分发**：能打包给非本机、非本人使用，含首次运行向导与模型下载器。

**非目标（本轮）**

- 移动端。
- 说话人分离（diarization）。
- 多语言源——听的方向仍只做 EN→ZH；说的方向支持中英混说。
- 云同步、账号体系。

## 3. 实测基准（2026-08-09，M2 / 16G，60s 英语讲话素材）

用本机已有的 large-v3-turbo 实测，`mlx_whisper 0.4.3`。脚本见 `docs/benchmarks/2026-08-09-mlx-whisper-latency.py`，下表取两次独立运行的区间：

| 块长 | 单次耗时 | 实时倍率 |
|---|---|---|
| 2s | 0.94–1.00s | 约 2x |
| 5s | 1.00–1.01s | 约 5x |
| 8s | 1.03–1.04s | 约 8x |
| 15s | 1.11–1.20s | 约 13x |
| 30s | 2.21–2.35s | 约 13x |

**三个结论，都直接决定架构：**

**(a) 短块不便宜。** 2s 与 15s 耗时几乎相同（约 1s），因为 Whisper 内部一律把 mel 补到 30 秒过 encoder。**推论：任何"高频小增量刷新"的设计都是错的。**

**(b) v2 的 partial 策略作废。** v2 设计要求"每 1s 对累积中的整句重转一次"。实测模拟一句 8 秒的话，累计 GPU 占用 13.8s / 17.7s（两次运行）对 8s 说话时间 = **占空比 172% / 221%**，永久性掉队。此设计必须删除。

**(c) 定稿式管线完全够用。** VAD 分句 + 每句只转一次：每句话说完约 **1.1 秒**出字，占空比约 20–25%，余量足够跑翻译。对比 v1 的 8 秒缓冲 + 约 10 秒滞后，这是能否跟住讲者的分界线。

**附带修复（必须写进实现）：** 默认参数下部分块会跳到 6–8 秒。查明是 Whisper 的 temperature fallback 阶梯（0.0→1.0 六档重试）。实测：

| 设置 | 3s 块耗时（两次运行） |
|---|---|
| 默认（带 fallback） | 8.19s / 10.84s |
| `temperature=0.0` | 0.96s / 0.98s |

实时管线必须固定 `temperature=0.0`，把最坏情况从 8–11s 压到约 1.0s。代价是放弃 fallback 的纠错，但实时场景下"慢 8 秒"比"偶尔差一句"严重得多。

## 4. 架构：五槽管线，模式即预设

两种模式的管线**形状相同**，只是每个槽绑不同实现。这是"融合"能成立而非硬拼的根据：

```
AudioSource → Segmenter → SttProvider → PostProcessor → Sink
```

| 槽 | 听（LiveScribe 模式） | 说（Dictation 模式） |
|---|---|---|
| AudioSource | 麦克风 \| 系统音频（BlackHole） | 麦克风 |
| Segmenter | VAD：600ms 静音定稿，30s 硬上限 | **按住说**：热键按下→抬起即一段<br>**开关式**：热键开启，之后由 VAD 分句 |
| SttProvider | 同一个（用户选） | 同一个（用户选） |
| PostProcessor | 翻译 EN→ZH | 润色：去口水词、加标点、套个人术语表 |
| Sink | 字幕面板 + 会话文件 | 光标处注入 |

> **2026-08-10 修正。** 本节原写作"Dictation 模式不需要 VAD——热键本身就是分段器"。这只对**按住说**成立。用户要求的**开关式连续口述**（按组合键开始、再按一次停止）必须有 VAD，否则口述三分钟就得等三分钟。VAD 因此是两个模式的共同依赖，P1 建它是无条件的。

### 4.1 听写模式完整规格（2026-08-10 定）

#### (a) 三层节奏必须解耦

原先把"转录 / 润色 / 注入"当成同一个节拍，这是错的。三者各走各的：

| 层 | 节奏 | 理由 |
|---|---|---|
| **转录** | **逐句**（VAD 切一段转一段） | 本地、免费、无限流。必须逐句才追得上说话速度 |
| **润色** | **逐段**（攒约 10–20 秒发一批） | 上下文够用、调用少、**边说边跑** |
| **注入** | **跟着润色走**，一段落定一次 | 见 (c) 铁律 |

**为什么润色不能逐句——首要原因是质量，不是限流。** 逐句时 LLM 只看到「而是一种重写」六个字，不知道"重写"接的是什么、你在写什么体裁，很可能"好心"改写成通顺但走样的句子。**润色质量正比于上下文长度，逐句是上下文最短的切法。** 这一条本身即足以否决它。

附带的账也难看：10 分钟口述，逐句约 130 次调用（12–15 次/分，有突发），逐段约 30–40 次（3–4 次/分）。Groq 免费档约 30 次/分，逐句在突发时顶线，逐段留十倍余量。

**批次边界不用固定秒数，用说话的自然节奏**：攒够 ≥3 句且遇到 >1.2s 停顿即发；一口气不停则 25s 强制发；按停止时强制清空队列。

**"边说边跑"是关键。** 润色不等你停下才开始，所以按下停止时队列里只剩最后一批——等待时间从"整篇 40 秒"变成"最后一段 3–5 秒"，**且与口述总长无关**。

#### (b) 润色队列串行，堵车自动合并

同时只允许一个润色请求在飞。否则第 3 段先于第 2 段返回就会打乱注入顺序。

串行带来一个自愈副作用：**LLM 变慢 → 后面积压 3 段 → 下次不发 3 个请求，发 1 个更大的 → 调用更少、上下文更长、质量更好 → 自己追上来。** 不需要额外补救逻辑。

上下文策略：润色第 N 段时只带**前一段**做参考，不带全文。**所以单次请求大小恒定，与口述总长无关**——上下文窗口不构成约束，10 分钟和 60 分钟一样。

#### (c) 三道降级防线

> **原则：转录结果一旦拿到就是安全的。润色是锦上添花，永远不许把花连着锦一起烧掉。**

| 出什么事 | 兜法 |
|---|---|
| 某批润色失败／超时 | **降级注入该批原始转录**。不是空白、不是报错 |
| 进程崩溃 | 原始转录持续写入 `~/Utter/sessions/`，重开可捞回。十分钟口述不会一无所有 |
| 注入失败（应用不接受、焦点已变） | 落**剪贴板** + 通知「已复制，按 ⌘V」。绝不静默丢字 |

#### (d) 注入机制

**长文本走剪贴板粘贴，不逐字模拟键盘。** 1500 词逐字打进 Word 又慢又易被应用打断。粘贴前备份用户原剪贴板，粘完还原。

#### (e) 目标窗口锁定与焦点离开时的缓冲

用户需求：开始听写后切走窗口，仍能继续说，文字最终落到正确的地方。

技术路径评估：

| 做法 | 判决 |
|---|---|
| 记住目标 app，注入前切回前台 | **可靠**，但会打断用户当前操作 |
| `CGEventPostToPid` 投递按键给指定进程 | **不可靠**——多数应用非前台时不处理按键，静默失败 |
| 辅助功能 API 直接改目标文本框的值 | **挑食**——原生 Cocoa 可，Electron（Obsidian/VS Code/Slack）、浏览器、终端基本不行 |

后两条属于"看着能用、实际随机失败"，**随机失败比明确失败更糟**（用户不会知道哪一段丢了）。采用方案：

```
开始听写 → 记下当前窗口（进程 + 应用名）为「目标」
说话中   → 焦点仍在目标   → 逐段正常注入
         → 焦点已切走     → 暂存不注入，浮窗标「N 段待落」
停止 / 切回目标 → 目标切回前台，缓冲区一次性粘贴
```

**依据：用户人不在那个窗口时，文字实时出现在那儿本来就没有价值（他看不见）。** 所以缓冲在感知上零成本，换来完全的可靠性——这不是妥协，是把无用功省掉。

目标窗口中途关闭 → 落剪贴板 + 通知。用户切回目标时自动冲刷缓冲，无需手动。

#### (f) 暂存模式（第三种，不绑定任何窗口）

开始听写后文字只进浮窗与缓冲区，**由用户决定何时、往哪里粘**。场景：一边读 PDF 一边口述批注，读完切到 Obsidian 一次粘入。

**实现上最简单（完全不需要注入逻辑），但可能是实际使用率最高的一个。**

#### (g) 术语与个人词表：三层，两层零成本

| 层 | 机制 | 代价 | 适合 |
|---|---|---|---|
| **1. Whisper `initial_prompt`** | 转录**之前**喂词表，偏置模型输出 | **零延迟** | Venuti、异化、CSSCI、人名 |
| **2. 词表替换** | 转录后确定性查找替换 | **零延迟** | 反复认错的固定错误 |
| 3. LLM 提示词 | 上下文一致性、语气、格式 | 0.3–1.5s | 成句整理 |

**重点在第 1 层**：让 Whisper 在源头听对 "Venuti"，而不是先听成 "venue tea" 再让 LLM 猜。**前两层关掉 LLM 也照样生效。** VoiceInk 的 personal dictionary 即此思路。

#### (h) 润色分档，默认最保守

**风险陈述：LLM 润色会改动原意，而作者可能不会发现。** 本项目作者口述的是学术论证；若「翻译不是复制，而是一种重写」被润色成「翻译是一种创造性的转换过程」——更通顺、更"像论文"，**但论点已被替换**，且因为读起来顺，回头校对时极难发现。

对策三条，缺一不可：

1. **提示词写死为保守指令**：只删口水词、补标点、分段；**不得改写措辞、不得增删内容**。
2. **分档**：轻（仅标点断句）／ 中（去口水词与重复）／ 重（整理成文）。**默认「轻」。**
3. **原始转录始终存档**。`~/Utter/sessions/` 同时保留原文与润色版。这是唯一能让作者事后发现问题的机制，成本近乎为零。

#### (i) 延迟预算

| 环节 | 耗时 | 依据 |
|---|---|---|
| VAD 判定停顿 | 400–600 ms | 可调 |
| Whisper 转录 | **~1.0 s** | **§3 实测** |
| LLM 润色 | 0.3–1.5 s | **估算，P2 需实测** |
| 注入 | ~50 ms | — |
| **合计** | **关润色 ≈ 1.5s ／ 开润色 ≈ 2–3s** | |

**润色必须可关。** 关掉是 1.5 秒的纯听写，开着是 2–3 秒的成稿。

#### (j) 为什么不照抄现有项目的"说完再插入"

调研（2026-08-10）：Typeless 为按住说→松开→插入润色文本，纯云端，第三方评测称单次会话上限 6 分钟；VoiceInk（GPL v3，Swift + whisper.cpp）支持按住与开关两种热键、本地转录、BYOK 的 AI enhancement、personal dictionary、按 app 切配置的 Power Mode；VocaMac 为按住右 Option 的最小实现。

**这个品类没有一个产品做逐字流式注入**，因为往他人应用注入是单向的——写进去就收不回，若 LLM 事后改词就得回删重打，风险不可控。**本项目同样不做逐字流式**，(a) 的逐段提交已满足"不必说完才出字"。

但"说完再一次性插入"不能照搬：

| | 那些工具 | 本项目 |
|---|---|---|
| 单次时长 | 10–60 秒（回消息、写邮件） | 1–10 分钟（口述论文段落） |
| "等说完"的代价 | 2 秒，**无问题可解** | 40 秒起，且全押在一次调用上 |
| 失败后果 | 重说一遍 | **10 分钟白说** |

Typeless 的 6 分钟上限本身即说明其设计假设是"短"。**结论不是他们做错了，是他们解的是一道更短的题。** 故本项目两套都要：按住说照抄成熟做法（该尺度下它就是最优解），开关式长口述走 (a)。

## 5. Provider 抽象

### 5.1 STT

```python
class SttProvider(Protocol):
    id: str
    display_name: str
    def is_available(self) -> tuple[bool, str]:   # 平台/依赖检测，失败给可读原因
    def transcribe(self, audio: np.ndarray, language: str | None) -> str: ...
```

| 实现 | 适用 | 说明 |
|---|---|---|
| `MlxWhisperProvider` | **Apple Silicon 专属** | 本机首选。GPU 推理，固定 `temperature=0.0` |
| `FasterWhisperProvider` | 跨平台 CPU / CUDA | Intel Mac、Windows、无 GPU 机器的回退。v1 已有代码可复用 |
| `CloudSttProvider` | 任意平台，BYOK | Groq（跑 large-v3-turbo，极低延迟）/ Deepgram。弱机器或极限延迟场景 |

> **注意区分「模型」与「运行格式」。** Whisper 本身跨平台，受限的是格式：MLX 仅 Apple Silicon（Metal GPU）；CTranslate2 覆盖各平台 CPU 与 NVIDIA CUDA；GGUF（whisper.cpp）几乎无所不跑，含老 Intel 机。**同一档模型在不同机器上下载不同格式即可。** provider 抽象的作用就是把这层差异藏起来——用户只选档位，格式由平台按硬件自动挑。
>
> **用户已确认持有非 Apple Silicon 设备**，故 `FasterWhisperProvider`（或 whisper.cpp 绑定）不是"以后再补"，**在 P1 就要与 MLX 一起进接口**，至少保证 catalog 与探测逻辑从第一天就是多后端的。

### 5.2 LLM（供翻译与润色共用）

> **BYOK = Bring Your Own Key（自带密钥）**：软件本身不含任何 API key，由使用者自己注册并填入。意义在于分发时你不替任何人付费、也不承担 key 泄漏风险。下文凡出现 BYOK 均指此。

| 实现 | 说明 |
|---|---|
| `OllamaProvider` | 本地，默认。全离线的那一半靠它 |
| `OpenAICompatProvider` | BYOK，一套接口覆盖 OpenAI / DeepSeek / Groq / 硅基流动等 |
| `FreeTranslateProvider` | deep-translator，仅翻译、无需 key，**仅作兜底** |

> v1 的教训要写进文档：deep-translator 的免费 Google 端点无 SLA，一节 90 分钟讲座数百次调用会被限流，v1 届时只静默写入 `[translation error]`。免费通道只能是兜底，不能是默认。

### 5.3 模型管理：用户只选档位，格式由平台决定

**用户看到的（不出现任何格式术语）：**

| 档位 | 说明 | 体积（Apple Silicon / 其他） |
|---|---|---|
| 高精度 | 最准，口音与专业术语都稳；需要较新的机器 | 3.08G / 3.09G |
| **均衡（推荐）** | 准确度接近高精度，但快得多。日常首选 | 1.61G / 1.62G |
| 轻量 | 一般机器也流畅，长词与术语偶尔出错 | 0.51G / 0.48G |
| 极轻 | 老机器或内存紧张时应急，准确度明显下降 | 0.08G / 0.15G |

> **体积数字于 2026-08-10 实测更正。** 本表原为估计值（1.5G / 0.8G / 0.5G / 0.15G），实查 HuggingFace API 后发现前两档约为真实值的一半。用户是看着这个数字决定要不要在计费网络下下载的，**估值不可接受**。现值为权重文件字节数之和，逐条见 `backend/catalog.py`。

> **一个要避免的误导：** 直觉上"权重大＝慢"，但 `large-v3-turbo` 是例外——它有 809M 参数却只有 4 层 decoder（large-v3 是 32 层），所以**又大又快**。这正是"均衡"档推荐它的原因，也是本机实测 1 秒出字的来源。档位文案不要写成"大＝慢、小＝快"的线性梯度。

**平台内部的映射（用户不可见）：** 首次运行探测硬件（Apple Silicon？NVIDIA GPU？仅 CPU？内存多少），再决定同一档下载哪种格式。

| 档位 | Apple Silicon | NVIDIA GPU | 仅 CPU |
|---|---|---|---|
| 高精度 | `whisper-large-v3-mlx` | faster-whisper `large-v3` (CUDA) | whisper.cpp `large-v3` 量化 |
| 均衡 | `whisper-large-v3-turbo` | faster-whisper `large-v3-turbo` | whisper.cpp `turbo` 量化 |
| 轻量 | `whisper-medium-mlx-4bit` | faster-whisper `small` | whisper.cpp `small` 量化 |
| 极轻 | `whisper-base-mlx-q4` | faster-whisper `base` | whisper.cpp `base` 量化 |

**非 Whisper 的候选**（catalog 应留位，不必首发全实现）：

- **Parakeet**（NVIDIA）——英语专精、极快，有 MLX 移植。纯英语听写场景可能优于 Whisper
- **SenseVoice / FunASR**（阿里）——中文显著强，**中英混说听写值得单列一档**
- **Moonshine**——极小、为端上流式设计，英语

模型管理器需要：档位目录、硬件探测、带进度的后台下载、磁盘占用显示、删除。**模型不能打进安装包**，一律首次运行时按需下载。

### 5.4 VAD：去掉 torch

v2 声明了 `silero-vad` pip 包，它拖入 **328M 的 torch**，而 v1 实际从未 import（评估已坐实为"纯陪葬"）。

v3 改用 **silero-vad 的 ONNX 权重 + onnxruntime**（约 2M）。这条路在本项目已被验证——v1 的 `faster-whisper` `vad_filter=True` 走的就是 onnxruntime。**净省 400M 依赖。**

## 6. 分发层（v2 完全没有的一整层）

**分发路线已定：GitHub 开源。** 不买 Apple Developer ID，$99 的公证留到"将来想做成正经 App"时再说。这条路是 OpenTypeless 一类项目的常规做法，成本为零：

- 同学**从源码构建** → 完全没有签名问题
- 你在 GitHub 发**预编译包** → 对方首次打开会被 Gatekeeper 拦，右键→打开、或一行 `xattr -d com.apple.quarantine` 即可，README 写清楚即可。开源界这是常态

| 事项 | 状态与决策点 |
|---|---|
| **sidecar 打通** | 当前必须手开两个终端（`build.sh` 的 PyInstaller 产物从未构建、`sidecar.ts` 是死代码、`externalBin` 已被移除）。评估判断这很可能就是 v1 从未被用起来的机械原因。**分发的前置条件，不可省。2026-08-10 起排期提前到 P2b**（浮窗需要 Tauri 窗口） |
| **首次运行向导** | 麦克风权限 → 辅助功能权限（光标注入必需）→ 选并下载 STT 模型 → 选 LLM（本地 Ollama / 填 key）→ 可选 BlackHole 引导 |
| **密钥存储** | BYOK 一律进 macOS Keychain，不落明文 JSON。包内不得含任何 key |
| **配置持久化** | v1 的 `PATCH /config` 只改内存、重启即失，Settings 页的 Save 是假象。v3 必须落盘 `~/Utter/config.json`（chmod 600） |
| **数据目录迁移** | `~/LiveScribe/` → `~/Utter/`。首次启动时若检测到旧目录即迁移（现存 3 个 v1 会话，v2 起 JSON 兼容，仅需补 `id` 字段）。**2026-08-10 实现时定为「复制」而非「搬迁」**：那 3 个会话是世上唯一副本、共 3KB，移动没有任何收益，而搬砸了就找不回来。旧目录留给作者自行删除。目标目录已存在时拒绝迁移，防止陈旧的 v1 树覆盖现役数据 |
| **脚手架残留** | `productName: "frontend"`、`identifier: "com.d.frontend"`、`csp: null`、模板 `greet` 命令——identifier 会烧进 .app 身份，分发后再改等于换应用，**打包前必须清** |

## 7. v2 计划的存废清单

| v2 内容 | v3 处置 |
|---|---|
| VAD 事件驱动管线 | **保留骨架**，但去掉 partial 分支 |
| `transcribe_partial(beam=1)` / `transcribe(beam=5)` 双档 | **删除**。①实测证明高频 partial 不可行 ②`beam_size` 是 faster-whisper 惯用法，不适用于 mlx 路径 |
| `silero-vad` pip（拖 torch） | **换成 ONNX + onnxruntime** |
| 可编辑条目（英文与译文双可编辑） | **保留**，设计完好 |
| 会话模型（Start/Pause/Resume/Stop、自动保存、重命名） | **保留** |
| `SessionHeader` / `StartDialog` / `History` 组件 | **保留**，听模式下仍需要 |
| 错误处理表（VAD 假阴性 30s 硬上限、翻译失败重试、WS 断连恢复） | **保留**，是好设计 |
| 单 provider 假设 | **重写**为 §5 的 provider 抽象 |
| 无分发层 | **新增** §6 |

v2 的 811 行计划里，会话与 UI 那部分（约六成）仍然有效；管线与配置层（约四成）需重写。

## 8. 分期路线

**P0 — 卫生（约 15 分钟）**
- 补提交仍 untracked 的 v1 两份奠基文档（2101 行，仓库现在只有 v2 没有 v1）
- ~~建私有远端~~ —— 用户明示暂缓
- 把 §3 基准脚本与数据落进 `docs/` ✅ 已完成

**P1 — 共享核心（1–2 天，纯后端，先不碰 GUI）**
- `SttProvider` / `LlmProvider` 接口 + 硬件探测 + 档位 catalog（**多后端从这里就要成立**）
- `MlxWhisperProvider`（本机）与 `FasterWhisperProvider`（非 Apple Silicon 设备）两个实现
- VAD（ONNX，不引 torch）+ 配置落盘 + Keychain 存 key
- 一个 CLI 跑通端到端，确认延迟与 §3 实测一致

**P2 — 先做听写模式（2026-08-10 拆为两步）**

**P2a — 能用的最小听写（无界面）**
- 全局热键（按住说）+ **暂存模式**（§4.1f，不绑窗口、无注入逻辑，实现最省）
- 说完 → 转录 → 可选润色（一次调用）→ 一次注入
- 词表前两层（§4.1g 的 `initial_prompt` 与替换表）、润色分档（§4.1h，默认「轻」）、原文双存档
- **默认关润色**（约 1.5s 出字）；配置改文件，不做设置界面
- **不做**：开关式、逐段批处理、窗口锁定、浮窗

> **排序理由：P2a 里没有一个未验证的假设，全是成熟做法。** 做完当天即可日用，且开始攒真实使用数据。P2b 的批次窗口、润色档位这些参数，只有拿真实使用经验才调得对；先做 P2b 等于在没数据的情况下拍参数。

**P2b — 长口述与平台整合**
- 开关式模式 + §4.1a 的逐段批处理 + §4.1b 的串行合并队列
- §4.1e 的目标窗口锁定与焦点离开缓冲
- 浮窗（实时显示原始转录）、菜单栏图标、词表与设置界面
- **`sidecar` 修通从 P4 提前到此**——P2b 需要 Tauri 窗口，若 sidecar 仍断，又变成开两个终端，即四月失败模式重演
- 中英混说模型实测（large-v3-turbo / SenseVoice / Parakeet）与润色端本地 vs 云延迟实测

> **排序理由：** 听写更简单（无 VAD、无翻译）、天天用（写论文即用）、且能端到端验证整条链路。**更重要的是它避开了 4 月的失败模式。**
>
> 已坐实的事实是：v1 完成于 2026-04-15，而 `~/LiveScribe/sessions/` 至今只有开发当晚（04-14 23:31–23:37）的 3 个测试会话，**一次真实课堂都没服务过**。原因至少有三层，评估已确认前两层——每次要开两个终端的启动摩擦、8 秒缓冲带来的约 10 秒滞后；第三层是推测但值得记下：四月中旬英国二学期已近结课，工具很可能生在没有课的季节里。**三层原因里有两层与"季节"无关，但第三层提醒我们：把交付时点押在学期开始前是有风险的。** 听写不看学期，做完当天就有反馈闭环。

**P3 — 重写听模式（2–3 天）**
- 按 §4 重写管线，复用 v2 保留下来的会话与 UI 设计
- System Audio 走通（BlackHole 检测 + `device_index` 连线——两端接口其实都已存在，只缺接线）

**P4 — 打包与分发（1–2 天 + 签名决策）**
- ~~sidecar 修通~~（已提前到 P2b）、模型下载器、首次运行向导、脚手架清理

## 9. 已决与待决

**已决（2026-08-09 用户确认）**

1. **平台定位**：融合式双模，不是两个工具。
2. **多后端从 P1 起**：用户持有非 Apple Silicon 设备，故 catalog 与硬件探测第一天就要多后端。
3. **分发走 GitHub 开源**，不买 Developer ID。
4. **先做听写模式**——用户当前"一直在打字"，这是即时痛点。
5. **暂不建远端仓库**（用户明示）。风险自担项：3080 行设计文档 + 本文目前仅本机一份。

6. **项目更名为 Utter。** 取语言学的 *utterance*（话语）——正是 §4 里两个模式共同的处理单元：听模式由 VAD 分句得到，说模式由热键分段得到。名字与领域模型同词。英文里 "utter" 作动词即"说出"。
   - 重名核查（2026-08-09 实查 GitHub）：`utterance/utterances` 9696★ 是网页评论组件，**领域无关**。落选的 Verba 撞 `weaviate/Verba` 7712★——同属 AI 工具领域，不可接受；Parole 撞 Xfce 媒体播放器且英文有"假释"歧义。

**已决（2026-08-10 用户确认）**

7. **听写三层节奏解耦**（§4.1a）：转录逐句、润色逐段、注入跟润色。逐句润色因**上下文过短伤质量**被否决，限流与成本只是附带理由。
8. **润色队列串行**（§4.1b），堵车自动合并；只带前一段做上下文，故请求大小与口述总长无关。
9. **三道降级防线**（§4.1c）：转录先于润色落定，润色失败降级原文，注入失败落剪贴板。**绝不静默丢字。**
10. **不做逐字流式注入**（§4.1j）。全品类无人这样做，因注入不可撤回；逐段提交已满足"不必说完才出字"。
11. **焦点离开时缓冲而非强注入**（§4.1e）。拒绝 `CGEventPostToPid` 与辅助功能直改值两条路——它们随机失败，比明确失败更糟。
12. **润色默认「轻」档 + 原文双存档**（§4.1h）。防的是 LLM 悄悄改动学术论证的原意。
13. **P2 拆 P2a / P2b**，`sidecar` 修复随 P2b 从 P4 提前。

**待决**

1. ~~目录改名~~ **已执行（2026-08-09）**：`live voice transform` → `utter`。
2. **应用标识符**：`com.d.frontend` → `com.dylan.utter`；`productName: "frontend"` → `"Utter"`。打包前必须改，identifier 会烧进 .app 身份。
3. **中英混说听写用哪档模型**——large-v3-turbo / SenseVoice / Parakeet 需实测比一次，排在 **P2b**。
4. **润色批次窗口的具体秒数**（§4.1a 暂定 ≥3 句 + >1.2s 停顿 / 25s 强制）。**刻意不在抽象中定死**——做成配置项，待作者用过 P2a 后按手感调。
5. **P2a 的热键组合**待定，需避开系统与常用应用的占用。
