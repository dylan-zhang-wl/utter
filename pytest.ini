[pytest]
testpaths = backend/tests
markers =
    network: hits the network; opt in with `pytest -m network`
    model: loads real model weights; opt in with `pytest -m model`
addopts = -m "not network and not model"
